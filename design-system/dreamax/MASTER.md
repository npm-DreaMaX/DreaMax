# DreaMax / Expedition

Reviewed 2026-09-23. The latest user direction is authoritative: replace the original homepage and its category presentation, learning from Adventure X throughout. Use the supplied background, its Orbix typeface and special X. The personal homepage remains the parent space; LLM Training, Agentic Scholar and Algorithm are peer destinations. Keep the real projects, tools and falling click strings. The original animated slogan belongs only on Join Us. The homepage quote is exactly “With resolve I depart; in greater glory I shall return.”, displayed on two lines in Cormorant Garamond 500 italic, approximately 40px desktop and 27px mobile. No Chinese quote remains. Remove the location/year labels and scroll prompt. The personal direction is “LLM training & RSI”. The former homepage card grids and research summaries are no longer mounted.

## Visual system

One full-height opening screen with the supplied warm light-trail image, a very large off-white Orbix wordmark and the font’s `ss02` X glyph. Orbix also appears in the two flagship stage titles. Inter and Noto Sans SC remain for body copy; Outfit for editorial headings. No blue nameplate, architectural placeholder background or left dot navigation.

Below the opening screen: a concise contents introduction and five anchor links; an ink-black LLM stage with a morphing 3D cube field and four training tabs; a vivid orange Agentic Scholar stage with interactive 3D orbital nodes and a four-step trajectory; a white Algorithm stage with step-by-step 3D bubble sort and actual article links; a dark project stage with stacked slabs, a triangular node network, and a waving block grid and keyboard-accessible tabs; an asymmetric tool exhibit; a full-width wordmark closing. Use black #151515, white #f4f3f0 and orange #ff712e as the main palette. Orange is a prominent section surface and interaction accent, not just a small highlight. Do not reintroduce green or sage. Keep the supplied image on the opening screen. Use literal section names and functional descriptions, without motivational marketing slogans. Preserve ordinary scrolling; never intercept the wheel or force scroll snapping. WebGL and SVG graphics are local illustrations, never represented as real training metrics. The bubble-sort demo executes real adjacent comparisons on an explicitly visible nine-number array.

## Navigation and motion

A transparent header over the first image becomes a dark navigation bar after scrolling. Direct peer links, an accessible learning-route disclosure, global search and the six-destination full-screen index remain. Native links and keyboard support are required. Restore focus after closing dialogs. Entry motion, image parallax and the original bouncing slogan on Join Us, recolored orange and white, respect reduced motion. The two-line homepage quote fades in once, with a slight stagger, then remains still; reduced motion disables its entrance. All important content is visible without JavaScript.

Algorithm uses the author’s 11 public CSDN algorithm articles with source links. Filters, search, sorting and URL state work together. No invented paper summaries from the old Algorithm page remain.

Peer pages also receive the new typography: Scholar uses expandable reading entries with primary sources; Tools uses an asymmetric illustration gallery and functional filters; Join Us uses the author’s supplied 2026-09-24 research statement and group photograph, in a reading order of LLM Training → Agentic RL → RSI → the personal military aspiration. The LLM Training page layout, sculpture, reading interactions and lesson content remain intact; its previous green tints are replaced with neutral surfaces and orange accents.

Four added WebGL instances use shared code: home training, home agents, home projects, and the Scholar header. Each scene loads near the viewport, caps rendering at 30fps and DPR at 1.5, stops when hidden, supports drag/keyboard/pause/expand/reset, and supplies a static SVG without WebGL. Reduced motion starts paused. Users may explicitly press play. Home scene state synchronizes with existing stage and project tabs. Tools and the portrait have pointer-controlled perspective; no forced scrolling.

## Sources and skill

User-requested UI UX Pro Max: https://github.com/nextlevelbuilder/ui-ux-pro-max-skill , pinned design reference `dcc40ff5133ef78276117db0cc34e7b83cc8aeba`. Apply keyboard support, touch targets, stable layout and reduced-motion guidance; the user’s visual reference takes priority over automatic palette suggestions.

Full asset provenance and Orbix license details: [docs/visual-sources.md](../../docs/visual-sources.md). The supplied bitmap is not represented as an original or open-source asset. Orbix is bundled under its verified OFL license. All application code is local implementation.

## Current 3D direction: selective rollback

The user rejected the sculptures in `3ab614b` and requested all 3D patterns return to `5e39c50`, while retaining the newer lighting, typography and background. Restore the exact voxel, orbital, slab, node and block-grid model implementations, original camera framing and rotation, static SVG fallback, and orange 3D sorting bars. The independent LLM Training entrance sculpture was not changed by that revision and remains intact.

Keep the studio softbox environment, first-screen photographic refraction, existing typography, black/white/orange palette, material/light/speed controls, image comparison, heading entrances and click strings. Material controls must support the restored MeshStandardMaterial models. Do not reintroduce the rejected metal ribbons, mechanical iris, interlocking rings or filament surface without a new request.

## Join Us · 2026-09-24

Use the user’s new Chinese statement and personal tribute as one continuous paragraph. The latest request replaces the separate research rows and large off-white photo section with a compact text-and-photo layout. Display the supplied group photograph at no more than 320 CSS pixels wide, preserving its full composition, captioned “照片左一是我。” (the position is supplied by the user). The old portrait is no longer displayed. The photo is static: no lightbox, image link, or open-original control. Keep the animated Join Us slogan, contact information, and resume link. Do not add invented personal history, dates, motivational slogans or claims of military service.
