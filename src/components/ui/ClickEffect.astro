<div class="click-code-layer" aria-hidden="true" inert data-click-code-layer></div>
<script>
  import { initClickEffects } from '../../scripts/click-effects';
  const layer = document.querySelector<HTMLElement>('[data-click-code-layer]');
  if (layer) initClickEffects(layer);
</script>
<style is:global>
  .click-code-layer { position:fixed; inset:0; z-index:80; overflow:clip; pointer-events:none; contain:strict; }
  .click-code-particle { position:absolute; white-space:nowrap; pointer-events:none; font:600 13px/1.4 ui-monospace,'Cascadia Code',monospace; text-shadow:0 0 12px currentColor; }
  .click-code-ripple { position:absolute; width:110px; height:110px; margin:-55px 0 0 -55px; border:1px solid #ff712e99; border-radius:50%; }
  @media(prefers-reduced-motion:reduce) { .click-code-layer { display:none; } }
</style>
