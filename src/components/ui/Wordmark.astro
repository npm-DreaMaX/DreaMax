---
interface Props { tagline?: boolean; }
const { tagline = false } = Astro.props;
---
<span class="dm-brand-text"><span class="orbix-wordmark">DreaMa<span class="orbix-x">x</span></span>{tagline && <small>LLM training &amp; RSI</small>}</span>
